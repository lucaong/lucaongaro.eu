<% wrap_layout :layout do %>
  <main>
    <article>
      <h1>
        <%= current_article.title %>
        <small><%= current_article.date.strftime('%b %e, %Y') %></small>
      </h1>

      <figure class="profile small">
        <%= image_tag 'luca_ongaro.png', alt: 'Luca Ongaro' %>
        <figcaption>
          I am Luca Ongaro, engineer and software developer. I love to write
          software and to craft products. When I grow up, I want to be a
          scientist.
        </figcaption>
      </figure>

      <%= yield %>
    </article>

    <aside class="tags">
      <ul>
        <% current_article.tags.each do |tag| %>
          <li><%= link_to tag, tag_path(tag) %></li>
        <% end %>
      </ul>
    </aside>
  <% end %>
</main>
